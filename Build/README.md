# Build metadata and source-controlled build inputs

This directory is reserved for source-controlled Bloodstream build inputs only (for example icons or platform metadata when they become real requirements).

Generated binaries, staged builds, packaged output and machine-specific build products must not be committed. Use `BuildOutput/` or Unreal's generated locations, which are ignored by repository policy.
